{
  "name": "wellkept",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "_dependencyNotes": {
    "bufferutil, utf-8-validate": "Optional native deps of `ws`, reached via @google/genai -> lib/gemini.ts. Nothing here imports them and a pruning pass will read them as dead weight. DO NOT REMOVE: without them webpack emits 'Module not found' and every request through the Gemini path HANGS INDEFINITELY rather than erroring. Cost an evening on 28 Jul. Guarded by lib/__tests__/ws-optional-deps.test.ts.",
    "jest, jest-environment-jsdom are pinned exact — do not restore the carets": "apps/mobile is on jest 29 (jest-expo 57 requires it), so a full workspace install hoists the jest-29 packages to the root and pushes the web app's jest 30 into node_modules/jest/node_modules. Those two are then resolved independently: the caret let `jest` float to 30.4.2 while `jest-environment-jsdom` stayed at 30.2.0, and the environment is where the moduleMocker comes from. jest-runtime 30.4 calls moduleMocker.clearMocksOnScope(), added in jest-mock 30.4 — against the 30.2 mock it is undefined, and EVERY web suite dies before its first test with `TypeError: this._moduleMocker.clearMocksOnScope is not a function`. Nobody noticed for weeks because the on-disk trees still held the old consistent 30.2.0 and nobody had installed apps/mobile. Pinning keeps runtime and environment on one version by construction. Bump them together or not at all, and verify with `rm -rf node_modules && npm ci && npx jest` — a passing `npx jest` on an existing tree proves nothing here.",
    "DO NOT run `npm run build` while `npm run dev` is running": "They share .next. A build replaces the chunks the dev server is serving, so every /_next/static request 404s: the page renders as unstyled HTML and React never hydrates, which presents as dead buttons rather than an error. This is the PackFileCacheStrategy / 503-chunk symptom recorded against /forgot-password. Fix: stop dev, rm -rf .next, restart. Deployments are unaffected — Netlify never runs dev."
  },
  "scripts": {
    "dev": "next dev",
    "dev:design": "NEXT_DIST_DIR=.next-design next dev",
    "build": "next build",
    "start": "next start",
    "build:verify": "NEXT_DIST_DIR=.next-verify next build",
    "start:verify": "NEXT_DIST_DIR=.next-verify next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "verify:mobile": "node scripts/verify-mobile-contract.mjs",
    "verify:mobile:strict": "node scripts/verify-mobile-contract.mjs --strict",
    "build:images": "node scripts/build-image-derivatives.mjs",
    "test:mobile": "npm test --workspace=@wellkept/mobile"
  },
  "dependencies": {
    "@google/genai": "^1.34.0",
    "@hookform/resolvers": "^3.9.0",
    "@next/swc-wasm-nodejs": "13.5.1",
    "@radix-ui/react-accordion": "^1.2.0",
    "@radix-ui/react-alert-dialog": "^1.1.1",
    "@radix-ui/react-aspect-ratio": "^1.1.0",
    "@radix-ui/react-avatar": "^1.1.0",
    "@radix-ui/react-checkbox": "^1.1.1",
    "@radix-ui/react-collapsible": "^1.1.0",
    "@radix-ui/react-context-menu": "^2.2.1",
    "@radix-ui/react-dialog": "^1.1.1",
    "@radix-ui/react-dropdown-menu": "^2.1.1",
    "@radix-ui/react-hover-card": "^1.1.1",
    "@radix-ui/react-label": "^2.1.0",
    "@radix-ui/react-menubar": "^1.1.1",
    "@radix-ui/react-navigation-menu": "^1.2.0",
    "@radix-ui/react-popover": "^1.1.1",
    "@radix-ui/react-progress": "^1.1.0",
    "@radix-ui/react-radio-group": "^1.2.0",
    "@radix-ui/react-scroll-area": "^1.1.0",
    "@radix-ui/react-select": "^2.1.1",
    "@radix-ui/react-separator": "^1.1.0",
    "@radix-ui/react-slider": "^1.2.0",
    "@radix-ui/react-slot": "^1.1.0",
    "@radix-ui/react-switch": "^1.1.0",
    "@radix-ui/react-tabs": "^1.1.0",
    "@radix-ui/react-toast": "^1.2.1",
    "@radix-ui/react-toggle": "^1.1.0",
    "@radix-ui/react-toggle-group": "^1.1.0",
    "@radix-ui/react-tooltip": "^1.1.2",
    "@supabase/ssr": "^0.9.0",
    "@supabase/supabase-js": "^2.58.0",
    "@tanstack/react-query": "^5.90.16",
    "bufferutil": "^4.1.0",
    "class-variance-authority": "^0.7.0",
    "clsx": "^2.1.1",
    "cmdk": "^1.0.0",
    "date-fns": "^3.6.0",
    "embla-carousel-react": "^8.3.0",
    "framer-motion": "^12.23.26",
    "input-otp": "^1.2.4",
    "lucide-react": "^0.446.0",
    "next": "13.5.11",
    "next-themes": "^0.3.0",
    "react": "18.2.0",
    "react-day-picker": "^8.10.1",
    "react-dom": "18.2.0",
    "react-hook-form": "^7.53.0",
    "react-resizable-panels": "^2.1.3",
    "recharts": "^2.12.7",
    "sonner": "^1.5.0",
    "tailwind-merge": "^2.5.2",
    "tailwindcss-animate": "^1.0.7",
    "utf-8-validate": "^6.0.6",
    "vaul": "^0.9.9",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@netlify/plugin-nextjs": "^5.15.1",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.1",
    "@testing-library/user-event": "^14.6.1",
    "@types/jest": "^30.0.0",
    "@types/node": "^20.6.2",
    "@types/react": "^18.2.22",
    "@types/react-dom": "^18.2.7",
    "autoprefixer": "^10.4.15",
    "eslint": "^8.49.0",
    "eslint-config-next": "^13.5.1",
    "jest": "30.2.0",
    "jest-environment-jsdom": "30.2.0",
    "postcss": "^8.4.30",
    "sharp": "^0.35.3",
    "tailwindcss": "^3.3.3",
    "typescript": "^5.2.2"
  }
}
